{
  "name": "expenses-budget-visualizer",
  "version": "1.0.0",
  "description": "Expenses & Budget Visualizer — client-side web app",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "fast-check": "^3.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
